/** Collapsed peek height of the fixed Enter Pulse sheet (keep in sync with TheLoEntryPanel). */
export const THE_LO_ENTRY_PANEL_PEEK_HEIGHT = "6.75rem";

/** Scroll buffer below footer content so it clears the fixed entry panel. */
export const THE_LO_SCROLL_CLEARANCE = `calc(${THE_LO_ENTRY_PANEL_PEEK_HEIGHT} + 2.5rem)`;
